{
  "name": "solsticio",
  "private": true,
  "devDependencies": {
    "@rollup/plugin-typescript": "^8.3.3",
    "c8": "^7.11.3",
    "nx": "^14.4.2",
    "typescript": "^4.7.4",
    "vite": "^2.9.14",
    "vitest": "^0.18.0"
  },
  "scripts": {
    "bootstrap": "pnpm i",
    "test": "pnpm --filter react --filter runtime test",
    "build": "pnpm --filter react --filter runtime build",
    "example-host": "pnpm --filter example-host-app dev",
    "example-plugins": "pnpm --filter example-plugins dev"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ]
}
